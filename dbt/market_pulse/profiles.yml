# dbt/market_pulse/profiles.yml
#
# Connection details are injected by the Airflow run_dbt task, which parses
# POSTGRES_URL and sets individual DBT_* env vars before calling dbt run.
#
# For local development set the vars manually or via a .env file:
#   export DBT_HOST=ep-xxx-xxx.region.aws.neon.tech
#   export DBT_PORT=5432
#   export DBT_USER=market_pulse_app
#   export DBT_PASSWORD=<password from Neon dashboard>
#   export DBT_DBNAME=market_pulse_dw

market_pulse:
  target: prod
  outputs:
    prod:
      type: postgres
      host:     "{{ env_var('DBT_HOST') }}"
      port:     "{{ env_var('DBT_PORT') | int }}"
      user:     "{{ env_var('DBT_USER') }}"
      password: "{{ env_var('DBT_PASSWORD') }}"
      dbname:   "{{ env_var('DBT_DBNAME') }}"
      schema: staging
      sslmode: require
      threads: 4
      connect_timeout: 30
